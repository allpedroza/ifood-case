# Ambiente local de desenvolvimento com Python 3.12.
# As dependências dos Jobs serverless ficam em src/databricks/resources/.
# PySpark não é instalado localmente: o pipeline usa pyspark.pipelines,
# disponibilizado pelo runtime Lakeflow Declarative Pipelines.

# Manipulação e análise local de dados tabulares
pandas>=2.2,<3.0

# Leitura local dos arquivos Parquet com pandas
pyarrow>=18,<24

# Kernel para usar este ambiente virtual em notebooks Jupyter/VS Code
ipykernel>=6.29,<8.0

# Requisições HTTP para baixar os arquivos da NYC TLC (src/01_extracao.py)
requests>=2.32,<3.0

# Extração determinística dos dicionários oficiais em PDF
pypdf>=6,<7

# Leitura e validação local do catálogo de regras de qualidade
pyyaml>=6,<7
